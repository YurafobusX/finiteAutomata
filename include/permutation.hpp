#pragma once

#include <vector>
#include <cstddef>

class permutation {
    permutation(const std::vector<size_t> in) : data(in){
    }
    std::vector<size_t> data;
    size_t size() {
        
    };
};